import 'package:test/test.dart';

void main() {
  group("Query observer", () {
    test("Should be called when a query is fetched", () {
      fail('not implemented');
    });
    test("Should be called when a query fails", () {
      fail('not implemented');
    });
    test('Should be called when a mutation is fetching', () {
      fail('not implemented');
    });
    test("Should be called when a infinite query is fetching", () {});
    test("Should be called when a infinite query has failed", () {});
  });
}
