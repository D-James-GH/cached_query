name: cached_query
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.7.0

dev_dependencies:
  test: ^1.20.2
  mockito: ^5.1.0
  flutter_lints: ^1.0.4
  dart_code_metrics: ^4.13.0
  build_runner:

