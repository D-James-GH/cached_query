import 'package:flutter_test/flutter_test.dart';

void main() {
  group("Creating query", () {
    test(
      "Query function only called once for multiple builders in the tree",
      () {},
    );
  });
}
