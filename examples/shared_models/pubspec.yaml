name: shared_models
description: Shared API models for cached_query examples.
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^3.2.3
  json_serializable: ^6.7.1
